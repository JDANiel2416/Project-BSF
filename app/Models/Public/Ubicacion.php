<?php
namespace App\Models\Public;

class Ubicacion {
    // Propiedades (id, latitud, longitud, descripcion, etc.)
    
    public function getCoords() {
        // Lógica para obtener las coordenadas de la base de datos.
        // $db = conectarDB();
        // $query = "SELECT lat, lng, zoom FROM ubicaciones WHERE id = 1";
        // ... ejecutar consulta ...
        // Retornaría un array con las coordenadas.
    }
}